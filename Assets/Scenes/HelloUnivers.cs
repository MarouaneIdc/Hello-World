using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloUnivers : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("hey");
    }

    // Update is called once per frame
    void Update()
    {
        print("univers");

    }
}
